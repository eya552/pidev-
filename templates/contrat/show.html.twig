
{% extends 'admin/Base.html.twig'%}

{% block body %}


<div style='margin-left: 20% ;'>
    <h1>contrat</h1>

    <div class="row" style="display: flex;align-items: center;justify-content: center;margin-top: 150px;">
        <div class="col-12">
            <div class="card my-4">
                <div class="card-header p-0 position-relative mt-n4 mx-3 z-index-2">
                    <div class="bg-gradient-primary shadow-primary border-radius-lg pt-4 pb-3">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contrat.id }}</td>
            </tr>
            <tr>
                <th>Idclient</th>
                <td>{{ contrat.idclient }}</td>
            </tr>
            <tr>
                <th>Idanimal</th>
                <td>{{ contrat.idanimal }}</td>
            </tr>
            <tr>
                <th>Datededebut</th>
                <td>{{ contrat.datededebut ? contrat.datededebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Datedefin</th>
                <td>{{ contrat.datedefin ? contrat.datedefin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Franchise</th>
                <td>{{ contrat.franchise }}</td>
            </tr>
             <tr>
                <th>Type de contrat</th>
                <td>{{ contrat.typedecontrat }}</td>
            </tr>
        </tbody>
    </table>
<div style=" margin-bottom: 10px;">
    <a href="{{ path('app_contrat_index') }}" class=" btn btn-dark">back to list</a>

    <a href="{{ path('app_contrat_edit', {'id': contrat.id}) }}" class=" btn btn-warning">edit</a>

    {{ include('contrat/_delete_form.html.twig') }}
{% endblock %}
 